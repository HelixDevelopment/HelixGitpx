apiVersion: v2
name: minio
description: MinIO object storage + bucket provisioning
type: application
version: 0.1.0
dependencies:
  - name: minio
    version: "14.8.4"
    repository: https://charts.bitnami.com/bitnami
