apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: adapter-pool
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "9"
  finalizers: [resources-finalizer.argocd.argoproj.io]
spec:
  project: default
  source:
    repoURL: "{{ env REPO_URL }}"
    targetRevision: main
    path: impl/helixgitpx/services/adapter-pool/deploy/helm
    helm:
      valueFiles:
        - values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: helix
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
