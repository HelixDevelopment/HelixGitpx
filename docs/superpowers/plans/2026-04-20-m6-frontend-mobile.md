# M6 Implementation Plan (compact)

Tasks:
- T1 — Web dashboard + repos routes
- T2 — PR, issue, conflicts-inbox routes
- T3 — search UI + upstream config UI + settings/members
- T4 — i18n 8 locales + a11y + PWA service worker
- T5 — KMP shared library expansion (SQLDelight, Ktor, stores)
- T6 — Compose Multiplatform shared screens
- T7 — Android + iOS + Desktop (3 platform) app entry points
- T8 — Distribution scripts (Play, F-Droid, App Store, TestFlight, MSIX, DMG, AppImage, .deb, .rpm)
- T9 — ADRs 0026-0029 + m6-frontend tag
